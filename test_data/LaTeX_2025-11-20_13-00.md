# LaTeX 基础入门教学

---
**Saved:** 2025/11/20 13:00:00  
**Source:** [Gemini Chat](https://gemini.google.com/app/b43382010cad5962?hl=zh-cn)  
---

## Gemini:

没错，就是 `_2_`！

`$\sqrt{16} = 4$` 是最标准、最清晰的写法。


- `$\sqrt 16 = 4$`（选项1）在某些 LaTeX 编译器里也能工作，但它只把 `1` 当作了 `\sqrt` 的内容，后面的 `6` 是碰巧跟在后面。使用 `{}` 把它写作 `\sqrt{16}` 是更安全、更准确的做法，能确保 `16` 始终是一个整体。



你现在已经掌握了 `\sqrt`！


---

接下来，我们来学习如何在 LaTeX 中输入**希腊字母 (Greek Letters)**，比如 α,β,γ 或 π。

这在数学和科学中非常常见。输入它们很简单，只需要在 `$` 符号内，输入一个反斜杠 `\` 加上这个字母的英文拼写。


- 要输入 α，就写作 `$\alpha$`


- 要输入 β，就写作 `$\beta$`


- 要输入 π (圆周率)，就写作 `$\pi$`



**轮到你啦：**

物理学中著名的“欧姆定律”是 V=IR。如果我们想用希腊字母 Ω (Omega) 来表示电阻单位“欧姆”，写成 R=5Ω。

你觉得应该怎么写？


1. `$R = 5 \Omega$`


1. `$R = 5 \omega$` (小写的 omega)


1. `$R = 5 O$` (大写的英文字母 O)

---

## Gemini:

不对哦，这个很接近了！

选项 `2` (`$\omega$`) 会显示小写的 omega，也就是 ω。

正确的答案是选项 `_1_`：`$\Omega$`。

在 LaTeX 中，希腊字母的大小写通常是由命令的**首字母大小写**决定的：


- `$\omega$` → ω (小写)


- `$\Omega$` → Ω (大写)




---

我们来练习一下这个概念。

**轮到你啦：**

假设你想输入大写的希腊字母 Δ (Delta)，你会用哪个命令？


1. `$\delta$`


1. `$\Delta$`

---

## You:

我觉得是2

---

## Gemini:

答对了！

就像我们之前学的 `\omega` (小写 ω) 和 `\Omega` (大写 Ω) 一样，希腊字母的大小写通常取决于你输入的命令本身是否大写：


- `$\delta$` → δ (小写 delta)


- `$\Delta$` → Δ (大写 Delta)




---

你已经掌握了希腊字母！我们来学一个新东西。

还记得我们最开始说的两种数学公式吗？


1. **行内公式** (我们一直在用，像 x+y 这样)


1. **独立公式**



我们目前只用了**行内公式** `$ ... $`。现在我们来学习**独立公式 (Display)**，它会让公式单独占一行并且居中，看起来更重要。

独立公式使用的是**两对**美元符号 `$$...$$`。

**轮到你啦：**

假设你想让著名的勾股定理 a2+b2=c2 单独居中显示，你觉得应该怎么写？


1. `$a^2 + b^2 = c^2$`


1. `$$a^2 + b^2 = c^2$$`


1. `{a^2 + b^2 = c^2}`

---

## You:

应该是2，我还想学习正负号怎么表示，还有乘除

---

## Gemini:

你答对了！`_2_` `$$a^2 + b^2 = c^2$$` 会创建独立的、居中的公式，非常醒目。

你提出了几个很棒的新问题！我们来逐个解决：正负号、乘法和除法。


---



### 1. 正负号 (Plus-Minus)



在 LaTeX 中，我们用 `\pm` (plus-minus) 命令来表示 ± 符号。

它经常用在二次方程的求根公式里。

**轮到你啦：**

如果想输入 x=±3，你觉得应该怎么写？


1. `$x = +- 3$`


1. `$x = \plusminus 3$`


1. `$x = \pm 3$`

---

## Gemini:

答对了！就是 `_3_`。

`\pm` (plus-minus) 是一个专门的命令，用来生成 ± 符号。


---



### 2. 乘法 (Multiplication)



在 LaTeX 中，表示乘法有好几种方式，最常见的有两个：


1. **叉乘 (times):** 使用 `\times` 命令，显示为 ×。


1. **点乘 (dot):** 使用 `\cdot` 命令，显示为 ⋅。



比如，要输入 2×3=6，你通常会写作 `$2 \times 3 = 6$`。
要输入 a⋅b，你会写作 `$a \cdot b$`。

**轮到你啦：**

如果想输入 F![](data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="0.471em" height="0.714em" style="width:0.471em" viewBox="0 0 471 714" preserveAspectRatio="xMinYMin"><path d="M377 20c0-5.333 1.833-10 5.5-14S391 0 397 0c4.667 0 8.667 1.667 12 5
3.333 2.667 6.667 9 10 19 6.667 24.667 20.333 43.667 41 57 7.333 4.667 11
10.667 11 18 0 6-1 10-3 12s-6.667 5-14 9c-28.667 14.667-53.667 35.667-75 63
-1.333 1.333-3.167 3.5-5.5 6.5s-4 4.833-5 5.5c-1 .667-2.5 1.333-4.5 2s-4.333 1
-7 1c-4.667 0-9.167-1.833-13.5-5.5S337 184 337 178c0-12.667 15.667-32.333 47-59
H213l-171-1c-8.667-6-13-12.333-13-19 0-4.667 4.333-11.333 13-20h359
c-16-25.333-24-45-24-59z"></path></svg>)=m×a![](data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="0.471em" height="0.714em" style="width:0.471em" viewBox="0 0 471 714" preserveAspectRatio="xMinYMin"><path d="M377 20c0-5.333 1.833-10 5.5-14S391 0 397 0c4.667 0 8.667 1.667 12 5
3.333 2.667 6.667 9 10 19 6.667 24.667 20.333 43.667 41 57 7.333 4.667 11
10.667 11 18 0 6-1 10-3 12s-6.667 5-14 9c-28.667 14.667-53.667 35.667-75 63
-1.333 1.333-3.167 3.5-5.5 6.5s-4 4.833-5 5.5c-1 .667-2.5 1.333-4.5 2s-4.333 1
-7 1c-4.667 0-9.167-1.833-13.5-5.5S337 184 337 178c0-12.667 15.667-32.333 47-59
H213l-171-1c-8.667-6-13-12.333-13-19 0-4.667 4.333-11.333 13-20h359
c-16-25.333-24-45-24-59z"></path></svg>) (一个矢量力等于质量乘以矢量加速度)，你会用哪个？


1. `$\vec{F} = m * \vec{a}$` (使用星号)


1. `$\vec{F} = m \times \vec{a}$`


1. `$\vec{F} = m x \vec{a}$` (使用英文字母 x)

---

## You:

应该是2吧

---

## Gemini:

你又答对了！就是 `_2_`。

`\times` 是 LaTeX 中用来表示 × 符号的标准命令。


- `*` (星号) 在 LaTeX 数学模式下也能显示，但它通常表示其他含义（比如指针），而不是标准的“乘号”。


- `x` (字母x) 会显示为 x，这在数学上是不对的。




---



### 3. 除法 (Division)



最后我们来看除法。你有两种主要方式来表示：


1. **分数 (Fraction):** 这是最常用的方式，你已经学会了！就是 `\frac{分子}{分母}`。例如 `$\frac{10}{2}$` 会显示 210​。


1. **除法符号 (Divide):** 如果你确实需要 ÷ 这个符号，你可以使用 `\div` 命令。



**轮到你啦：**

如果想输入 10÷2=5，你觉得应该怎么写？


1. `$10 / 2 = 5$` (使用斜杠)


1. `$10 \div 2 = 5$`


1. `$\frac{10}{2} = 5$`

---

## Gemini:

这个问题有点“狡猾”！你选的 `_2_` (`$10 \div 2 = 5$`) 是完全正确的，它能准确地打出 ÷ 符号。

不过，选项 `_3_` (`$\frac{10}{2} = 5$`) 也是**完全正确**的！它使用了你学过的分数 `\frac` 来表示除法。

在 LaTeX 中，使用分数 210​ (选项3) 往往比使用 ÷ 符号 (选项2) 更常见。

选项 `_1_` (`$10 / 2 = 5$`) 使用斜杠，也能显示，但它在排版上不如前两个那么专业。

所以，`_2_` 和 `_3_` 都是很好的答案！


---

**[ 快速回顾 ]**

你已经学了好多基础知识了！我们来总结一下：


- **两种模式:**


- `$...$` (行内公式)


- `$$...$$` (独立公式)




- **常用符号:**


- `^` (上标)


- `_` (下标)


- `{}` (分组)


- `\pm` (正负号 ±)




- **数学命令:**


- `\frac{分子}{分母}` (分数)


- `\sqrt{...}` (平方根)


- `\alpha`, `\Omega`... (希腊字母)


- `\times` (乘号 ×), `\cdot` (点乘 ⋅)


- `\div` (除号 ÷)





你已经打下了非常坚实的基础！

接下来你想学点什么？


1. **括号和定界符** (比如如何让括号自动变大: (21​) )


1. **求和与积分** (比如 ∑ 和 ∫)


1. **矩阵** (排列数字)

---

## You:

1和2都要学

---

## Gemini:

没错！你关于除法（÷）的答案 `_2_` 是完全正确的。

就像我们之前提到的，`$10 \div 2 = 5$` (选项2) 和 `$\frac{10}{2} = 5$` (选项3) 都是对的，而且用分数 `\frac` 在 LaTeX 中更常见。


---

你提出要学 **1 (括号)** 和 **2 (求和/积分)**，非常好！这两个是进阶必备的。

我们先从 **1. 括号和定界符** 开始，因为它在很多复杂公式（包括积分）里都会用到。

**问题：**
在 LaTeX 中，如果你只使用普通的小括号 `( )`，它们的大小是**固定**的。
比如，你输入 `$(\frac{a}{b})$`，它显示出来是 (ba​) —— 括号太小了，没有完全包住分数。

**解决方法：**
为了让括号能**自动调整大小**来包住里面的内容，我们要在左括号前使用 `\left`，在右括号前使用 `\right`。

例如：`$\left( \frac{a}{b} \right)$`
它就会正确地显示为 (ba​)。


---

**轮到你啦：**

假设你想输入一个包含分数的平方根 yx​![](data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="400em" height="1.88em" viewBox="0 0 400000 1944" preserveAspectRatio="xMinYMin slice"><path d="M983 90
l0 -0
c4,-6.7,10,-10,18,-10 H400000v40
H1013.1s-83.4,268,-264.1,840c-180.7,572,-277,876.3,-289,913c-4.7,4.7,-12.7,7,-24,7
s-12,0,-12,0c-1.3,-3.3,-3.7,-11.7,-7,-25c-35.3,-125.3,-106.7,-373.3,-214,-744
c-10,12,-21,25,-33,39s-32,39,-32,39c-6,-5.3,-15,-14,-27,-26s25,-30,25,-30
c26.7,-32.7,52,-63,76,-91s52,-60,52,-60s208,722,208,722
c56,-175.3,126.3,-397.3,211,-666c84.7,-268.7,153.8,-488.2,207.5,-658.5
c53.7,-170.3,84.5,-266.8,92.5,-289.5z
M1001 80h400000v40h-400000z"></path></svg>)​，并且想用**方括号 `[ ]`** 把它括起来，让方括号自动变大。

正确的写法应该是下面哪一个？


1. `$[\sqrt{\frac{x}{y}}]$`


1. `$\left[ \sqrt{\frac{x}{y}} \right]$`


1. `$\left( \sqrt{\frac{x}{y}} \right)$` (注意这是圆括号)

---

## You:

是2，我还有一个疑问，数学公式中可能会出现多层括号，而小括号，中括号，大括号总共才三个层级，似乎不够用

---

## Gemini:

你答对了！就是 `_2_` `$\left[ \sqrt{\frac{x}{y}} \right]$`。


---

你提出了一个非常好的问题！关于括号层级不够用的情况。

你说的没错，我们常用的只有 `()`、`[]` 和 `{}`。


1. **关于 `{}`**：在 LaTeX 中，`{}` 用来分组，如果你想让它作为括号显示，你需要“转义”(escape)它，写作 `\{` 和 `\}`。


1. 例如：`$\left\{ \frac{a}{b} \right\}$` 就会显示 {ba​}。




1. **关于“不够用”**：在数学排版中，当超过三层时，规范的做法是**循环使用**它们。


1. 例如，你可以写：
`$$\left[ \left( \left\{ \frac{a}{b} \right\} + c \right) \times d \right]$$`
显示为：


[({ba​}+c)×d]
1. 因为 `\left` 和 `\right` 会自动调整它们的大小，所以即使是重复的括号，读者也能通过大小清楚地分辨出层级。





你对括号的疑问解决了吗？如果解决了，我们就开始学习你刚才提到的第二项：**求和与积分**。

---

## You:

yeah，go ahead

---

## Gemini:

没问题！我们开始学习 **2. 求和与积分**。

我们先从**求和 (Summation)** 开始，它在 LaTeX 中用 `\sum` (sigma) 命令表示。

求和符号 ∑ 经常需要**下标**（起始点）和**上标**（结束点）。你猜的没错，我们又要用 `_` 和 `^` 了！

例如，要输入 ∑i=1n​ (从 i=1 加到 n)，我们写作：`$\sum_{i=1}^{n}$`


- `\sum` 生成 ∑ 符号


- `_` 把 `i=1` 放在下标


- `^` 把 `n` 放在上标




---

**轮到你啦：**

如果想输入一个从 k=1 加到 k=10 的求和公式 ∑k=110​k2，你觉得应该怎么写？


1. `$\sum_{k=1}^{10} k^2$`


1. `$\sum^{10}_{k=1} k^2$`


1. `$\sum_{k=1}^10 k^2$`

